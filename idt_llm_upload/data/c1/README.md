# C1 — source withheld

The 58 human–LLM conversations are personal correspondence and are not released.

Derived results are complete and are in `results/contribution/`:
`c1_nulls.csv`, `c1_controls.csv`, `c1_cos.csv`, and the C1 rows of `prefix.csv`.
These contain no message text. Record identifiers are opaque hashes
(for example `claude_05614dd8`).

Closure readings for C1 are in `results/closure/omega_w1_c1.csv` and `sweep_c1.csv`.
